const CurrText = ({currAudioText})=> {

    return (
        <div className="text-container">
        <h1 className="btn btn-success d-block w-100 mt-2">{currAudioText}</h1>
        </div>
    );


};

export default CurrText;